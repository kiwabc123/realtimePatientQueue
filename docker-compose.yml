version: '3.8'

services:
  app:
    build: .
    container_name: patient-queue-app
    ports:
      - "3001:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
